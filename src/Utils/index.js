import { Routes, Load as loadRoutes } from './Routes';
import { Styles, Load as loadStyles } from './Styles';

const load = {
  Routes: loadRoutes,
  Styles: loadStyles
};

export {
  load,
  Routes,
  Styles
};